# Per-run scratch written by pipeline steps — belongs on the Trello card and in
# the issue thread, never in this (public) repo. See Learnings log 2026-08-24.
*STEP*REPORT*.md
drafts_*.txt

# Brand-capture sweep scratch (CLO-95). These four were tracked at the repo root
# until 2026-08-25; the sweep tooling now writes them to PAPERCLIP_RUN_SCRATCH_DIR
# instead, so a repo-root copy is only ever a local interactive artifact.
# CLO-*-cards.json is sweep *input* and carries prospect email addresses.
*-capture-results.json
CLO-*-cards.json
move-instructions.json
trello-move-map.json
